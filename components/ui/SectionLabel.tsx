export function SectionLabel({ children }: { children: React.ReactNode }) {
  return (
    <p className="text-xs text-[#616A5C] opacity-70 uppercase tracking-wide font-semibold">
      {children}
    </p>
  );
}
